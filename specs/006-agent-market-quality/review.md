# Agent 市场质量体系评审

结论：ALLOW  
时间：2026-08-23 17:32 +08:00

## 两轮评审

### 第一轮：正确性、闭环与兼容性

- 质量事实写入只追加 `agent_metric_events`，业务回调、验收、争议、反馈和管理员操作均使用稳定幂等边界；重复事件会触发读模型修复，但不会重复计分。
- 评分模块统一实现贝叶斯可靠性、60 天半衰期、40/25/20/15 质量来源、交付、响应、历史、风险和置信度；市场、候选、邀请及工作流确认共用同一准入函数。
- 生命周期使用 75 上架、65 降级、50 暂停并保留 65–74 上架滞回区间；严重风险解除后仍需 Endpoint 健康、分数恢复和最近 3 单连续成功。
- `agentmesh.trial.v3` 分场景验证结构化执行、错误处理、artifact 契约和工程能力；旧 Agent 在 shadow 模式保持兼容，enforce 必须等待正式 Trial。
- 无效交付、结算与争议回放、反馈改版及同一任务方连续评价均有独立事件语义；Agent 详情保留历史可访问性。

### 第二轮：安全、权限与运营边界

- Agent Bearer 只从 Worker Secret 读取；Trial 使用 Agent ID 与独立 challenge 绑定，响应若泄露实际凭据或 token 模式会失败并写严重安全事件，原始响应和模型原文不落 D1。
- 反馈只允许任务方对已完成、已释放托管且无退款/有效争议的真实参与节点提交；敏感凭据模式被拒绝，版本历史保留且仅最新版本计分。
- 管理员不能直接改写统计分，只能追加带原因且支持 API 幂等的风险、解除风险或校正事件；前端提供产品化确认弹窗。
- scheduled health 将 401/403 判定为凭据健康失败；Endpoint 解析、响应大小、结构化协议和身份不一致均 fail closed。
- 未发现阻塞或 P0/Critical 问题。外部 `codex review --uncommitted` 因超大脏工作区无法可靠限定本功能范围，未作为通过证据；结论来自两轮人工审查与自动化验证。

## Reviewer 输出

```json
{
  "mode": "reviewed",
  "taskId": "T8",
  "verdict": "ALLOW",
  "issues": [],
  "p0Issues": [],
  "summary": "Agent 市场质量 Phase 1–5 的事件账本、Trial、评分、统一准入、反馈和运营安全边界满足首发需求，未发现阻塞或 P0 问题。"
}
```
