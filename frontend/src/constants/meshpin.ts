import { BRAND } from './brand';

export const MESHPIN_PUBLIC_ASSET = Object.freeze({
  name: BRAND.contribution.name,
  symbol: BRAND.contribution.symbol,
  testName: BRAND.contribution.testName,
  testSymbol: BRAND.contribution.testSymbol,
  purpose: BRAND.contribution.purposeLabel,
  disclaimer: BRAND.contribution.disclaimer,
});
